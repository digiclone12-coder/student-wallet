# Copy to .env and fill in. NEVER commit the real .env.
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?appName=<app>
DB_NAME=ndear_wallet
PORT=4000
ADMIN_API_KEY=change-me-in-production
